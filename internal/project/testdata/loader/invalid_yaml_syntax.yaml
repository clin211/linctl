apiVersion: linctl.dev/v1
kind: Project
metadata:
  name: hello
  module: github.com/foo/hello
spec:
  components:
    - kind: WebServer
      name: hello
       framework: gin # 缩进多一格 → YAML 语法错
      storage: memory
   bad_indent: this line has wrong indent
