apiVersion: linctl.dev/v1
kind: Project
metadata:
  name: hello
  module: github.com/foo/hello
spec:
  components:
    - kind: WebServer
      name: same
    - kind: Worker
      name: same
      variants: [cron]
      cron:
        jobs:
          - name: a
