apiVersion: linctl.dev/v1alpha1
kind: Project
metadata:
  name: legacy-app
  module: github.com/foo/legacy
spec:
  defaults:
    framework: gin
    storage: gorm-postgres
    apiVersion: v1
  components:
    - kind: WebServer
      name: legacy-app
